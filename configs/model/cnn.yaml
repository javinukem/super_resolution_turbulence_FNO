name: cnn
module: src.model.models_edsr
class_name: EDSR
params:
  input_channels: 5
  n_resblocks: 5
  n_feats: 64
  kernel_size: 3
  scale: 4
  activation_f: relu
tag_template: "res_{n_resblocks}_feats_{n_feats}"
